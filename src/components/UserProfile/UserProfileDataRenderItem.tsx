import { Pressable, StyleSheet, Text, View } from "react-native";
import React from "react";

export const UserProfileDataRenderItem = () => {
  return (
    <Pressable>
      <Text>UserProfileDataRenderItem</Text>
    </Pressable>
  );
};

const styles = StyleSheet.create({});
